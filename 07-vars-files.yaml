- name: play levael variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
    - name: print course information
      ansible.builtin.debug:
        msg: "Hello Iam learning{{COURSE}},  Course Duration: {{DURATION}}, Trainer is {{TRAINER}}"